{
  "port": 8080,
  "databases": {
    "postgres": "postgres://postgres:%s@localhost:5432/brigade_service?sslmode=disable",
    "kafka": {
      "brokers": [
        "localhost:9092"
      ],
      "topics": {
        "tasks": "tasks-topic"
      }
    }
  },
  "cluster": {
    "userService": "http://localhost/api/user-service"
  }
}
